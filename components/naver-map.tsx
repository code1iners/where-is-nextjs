const NaverMap = () => {
  return (
    <div
      id="map"
      className="absolute"
      style={{
        width: "100%",
        height: "100vh",
      }}
    />
  );
};

export default NaverMap;
